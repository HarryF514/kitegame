#pragma strict
//var MainGameModel = GetComponent('MainGameModel');
//var other;


function Start () {
}

function Update () {
	
}

//create another background if the camera reach the edage of the 
//if the number of background is less than 2, we will create one and append it at the end of last background, 
//if the number o background is more than 2, we will destory the first one,
function createBackground(){
	//first test
	//second test             
	//third test
}

//determin the type of the os
//return mobile if they are iphone, ipad ect any device that have a touch function
function isMobileDevice(){}
